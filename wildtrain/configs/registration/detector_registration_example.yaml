
classifier:
  weights: null
  processing:
    batch_size: 8 # used for onnx export
    export_format: "torchscript" # torchscript or onnx

localizer:
  yolo:
    weights: "/home/fadelmamar.seydou/workspace/wildetect/checkpoints/localizer.pt"
    imgsz: 800
    device: "cuda"
    conf_thres: 0.1
    iou_thres: 0.3
    max_det: 300
    overlap_metric: "IOU"
    merging_method: "NMS"
    task: "detect"
  processing:
    export_format: "pt"
    batch_size: 32
    dynamic: false

processing:
  name: "detector"
  mlflow_tracking_uri: "http://localhost:5000"
